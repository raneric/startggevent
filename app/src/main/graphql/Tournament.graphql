query TournamentsByCountry($perPage: Int!) {
    tournaments(query: {
        perPage: $perPage
    }) {
        nodes {
            id
            name
            countryCode
            eventRegistrationClosesAt
            images {
                url
            }
        }
    }
}